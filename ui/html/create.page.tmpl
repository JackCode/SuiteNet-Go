{{template "base" .}}

{{define "title"}}Create Work Order{{end}}

{{define "body"}}
<form action='/engineering/workOrder/create' method='POST' autocomplete="off">
    <input type='hidden' name='csrf_token' value='{{.CSRFToken}}'>
    <div>
        <h2>Create Engineering Work Order</h2>
    </div>
    <div>
        <label>Title:</label>
        {{with .Form.Errors.Get "title"}}
            <label class='error'>{{.}}</label>
        {{end}}
        <input type='text' name='title' value='{{.Form.Get "title"}}'>
    </div>
    <div>
        <label>Location:</label>
        {{with .Form.Errors.Get "location"}}
            <label class='error'>{{.}}</label>
        {{end}}
        <select name="location">
            {{range .Locations}}
            <option value="{{.ID}}">{{.Title}}</option>
            {{end}}
        </select>
    </div>
    <div>
        <label>Note:</label>
        {{with .Form.Errors.Get "note"}}
            <label class='error'>{{.}}</label>
        {{end}}
        <textarea name='note'>{{.Form.Get "note"}}</textarea>
    </div>
    <div>
        <input type='submit' value='Create Work Order'>
    </div>
</form>
{{end}}